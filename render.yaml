services:
  - type: web
    name: clandraft
    runtime: static
    # RaidStats.csv stays in the repo as the raw data store but is stripped
    # from the published site.
    buildCommand: "rm -f RaidStats.csv"
    staticPublishPath: .
    pullRequestPreviewsEnabled: false
